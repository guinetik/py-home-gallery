# =============================================================================
# Py Home Gallery - Docker Configuration
# =============================================================================
# Copy this file to .env and customize the values for your setup.
# Example: cp .env.example .env

# -----------------------------------------------------------------------------
# REQUIRED: Media and Thumbnail Directories
# -----------------------------------------------------------------------------
# Absolute path to your media directory on the host machine
# Windows example: MEDIA_DIR=F:/apps/gallery/media
# Linux/Mac example: MEDIA_DIR=/home/user/media
MEDIA_DIR=/path/to/your/media

# Absolute path to store generated thumbnails on the host machine
# Windows example: THUMBNAIL_DIR=F:/apps/gallery/thumbnails
# Linux/Mac example: THUMBNAIL_DIR=/home/user/thumbnails
THUMBNAIL_DIR=/path/to/your/thumbnails

# -----------------------------------------------------------------------------
# OPTIONAL: Server Configuration
# -----------------------------------------------------------------------------
# Port to expose on the host (default: 8000)
HOST_PORT=8000

# Items per page (default: 50)
ITEMS_PER_PAGE=50

# -----------------------------------------------------------------------------
# OPTIONAL: Cache Configuration
# -----------------------------------------------------------------------------
# Enable caching (default: true)
CACHE_ENABLED=true

# Cache TTL in seconds (default: 300 = 5 minutes)
CACHE_TTL=300

# -----------------------------------------------------------------------------
# OPTIONAL: Background Worker Configuration
# -----------------------------------------------------------------------------
# Enable background thumbnail generation (default: true)
WORKER_ENABLED=true

# Number of worker threads (default: 2)
WORKER_THREADS=2

# -----------------------------------------------------------------------------
# OPTIONAL: Logging Configuration
# -----------------------------------------------------------------------------
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# OPTIONAL: Content Customization
# -----------------------------------------------------------------------------
# Customize all UI text, titles, descriptions, and labels
# Useful for themed galleries (weddings, events, family collections, etc.)
#
# Method 1: JSON in environment variable (recommended for Docker)
# Provide JSON string directly (must be valid JSON, use single quotes to wrap)
# Note: For Docker, use the short names below (they get mapped to PY_HOME_GALLERY_* automatically)
# For direct Python execution, use the full PY_HOME_GALLERY_CONTENT_JSON name
#
# Method 2: Path to content.json file
# CONTENT_PATH=/path/to/custom/content.json
#
# Examples (uncomment and customize one):
#
# Wedding Gallery:
# CONTENT_JSON='{"site":{"title":"Sarah & John Wedding"},"home":{"hero_title":"Our Special Day","hero_subtitle":"June 15, 2024"},"navigation":{"site_name":"Our Wedding"}}'
#
# Family Photos:
# CONTENT_JSON='{"site":{"title":"The Smith Family"},"home":{"hero_title":"Family Memories","hero_subtitle":"Our precious moments together"}}'
#
# Corporate Event:
# CONTENT_JSON='{"site":{"title":"Tech Summit 2024"},"home":{"hero_title":"Tech Summit 2024","hero_subtitle":"Innovation and Ideas"}}'
#
# Minimal Example (just change the title):
# CONTENT_JSON='{"site":{"title":"My Gallery"},"navigation":{"site_name":"My Gallery"}}'
