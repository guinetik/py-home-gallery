# Core web framework
Flask>=3.0.0

# Image processing (11.0.0+ supports Python 3.13)
Pillow>=11.0.0

# Video processing
# moviepy 1.0.3 is old but stable, requires decorator<5.0
moviepy==1.0.3
decorator>=4.0.2,<5.0
imageio>=2.34.0
imageio-ffmpeg>=0.5.0

# Numerical processing (2.0.0+ supports Python 3.13)
numpy>=2.0.0

# Utilities
tqdm>=4.66.0

# Audio processing (moviepy dependencies)
pydub>=0.25.0

# Computer vision (4.10.0+ supports Python 3.13)
opencv-python>=4.10.0

# Optional: Production WSGI server (install with: pip install waitress)
# waitress>=3.0.0
